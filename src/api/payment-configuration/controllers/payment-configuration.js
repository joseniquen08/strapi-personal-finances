'use strict';

/**
 * payment-configuration controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::payment-configuration.payment-configuration');
